version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - misspell
    - unconvert
    - usestdlibvars
  settings:
    errcheck:
      check-type-assertions: false

formatters:
  enable:
    - gofmt
